block('select').elem('button')(
    def()(function() {
        var mods = this.mods;
        applyCtx({
            block : 'button',
            mix : { block : this.block, elem : this.elem },
            mods : {
                size : mods.size,
                theme : mods.theme,
                disabled : mods.disabled,
                checked : mods.type !== 'radio' && !!this._checkedOptions.length
            },
            content : apply('content')
        });
    })
)
